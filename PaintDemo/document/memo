 이전에 한 것들 예제



* JList ---> 컴포넌트
  ListModel -> 데이터, JList를 갱신시켜주는 기능
  
* JFrame - 윈도우상에서 
  JApplet - 웹 브라우저에서
  
* 더블 버퍼링(메모리(버퍼)에서 터치)
  method paint(Graphics g) --> g변수는 메인 UI를 터치함.
  1.백그라운드 그래픽 영역(버퍼)에 그림을 그린다.
  2. 버퍼의 이미지를 메인 UI에 전달.
  단점 : 메모리 사용향이 2배로 증가
  장점 : 빠르고 화면 깜빡임이 줄어들게 됨
  미리 안보이는 임시 버퍼(백그라운드)에 그려놨다가 Graphics g변수에 
  던져주면 Thread.sleep(프로그램에서 터치)을 사용했을 때 보다 깜빡임이 덜하다.